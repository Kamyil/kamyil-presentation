
import * as $ from "jquery";
import { $body } from "../Core/Body";         

export const AboutMe = (() => {
  const Selector = {
    YOUR_SELECTOR: 'document'
  }

  function showComponentStateInConsole() {
      
  }

$body.ready(showComponentStateInConsole);
})();
        